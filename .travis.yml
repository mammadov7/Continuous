language: c

os: linux


addons:
  apt:
    packages:
      - gcovr
      - libcunit1-dev

script:
  - mkdir Build
  - cd Build
  - cmake .. -DCMAKE_INSTALL_PREFIX=../inst
  - make
  - ctest --verbose
  - gcovr