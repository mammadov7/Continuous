language: C

os: Linux

dist: xenial

addons:
  apt:
    packages:
      - libcunit1-dev

script:
  - mkdir Build
  - cd Build
  - cmake .. -DCMAKE_INSTALL_PREFIX=../inst
  - make install
  - ../inst/bin/tp_area
